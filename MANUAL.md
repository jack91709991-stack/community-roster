# 自治会員名簿管理システム 初回設定・利用マニュアル

本マニュアルは、自治会・町内会の役員向けに、会員名簿管理システムの初期設定（役員合言葉の入力、スプレッドシート連携）を解説したガイドブックです。

---

## 🌐 システム公開URL

- **WebアプリURL**:  
  [https://jack91709991-stack.github.io/community-roster/](https://jack91709991-stack.github.io/community-roster/)
- **配布・A4印刷用マニュアルページ（画像付き）**:  
  [https://jack91709991-stack.github.io/community-roster/manual.html](https://jack91709991-stack.github.io/community-roster/manual.html)

---

## 📲 STEP 1: スマホのホーム画面に追加（推奨）

本システムは、ブラウザだけで動く最新のWebアプリ（PWA）です。アプリストアからの面倒なインストール作業は一切不要です。

1. **iPhone（Safari）の場合**:  
   ブラウザ下部の「共有ボタン（四角から上矢印）」をタップ ➜ **「ホーム画面に追加」** を選択。
2. **Android（Chrome）の場合**:  
   右上のメニュー（縦の3点リーダー）をタップ ➜ **「ホーム画面に追加」** または **「アプリをインストール」** を選択。

> ホーム画面にアイコンが作成され、次回からアプリアイコンをタップするだけで全画面起動できます。

---

## 🔒 STEP 2: 「役員合言葉（パスコード）」の入力と画面起動

個人情報保護のため、初回アクセス時や年度交代時には「役員認証」画面が表示されます。  
**また、いつでも画面右上の【🔒 未認証】ボタン（または【👤 役員】バッジ）をクリックすることで、合言葉の入力画面を起動できます。**

| 権限ロール | 初期合言葉 | 主な権限・役割 |
| :--- | :--- | :--- |
| **👤 一般役員** | `roster2026` | **閲覧・集金・帳票印刷専用（安全モード）**<br>・名簿の閲覧（電話・住所・家族構成等）<br>・「ブロック長・班長名簿」などのA4帳票印刷<br>・会費集金チェック（即時反映）<br>※名簿編集・削除や各種設定は誤操作防止のためロックされます |
| **👑 管理者** | `admin2026` | **全機能（フルアクセス）**<br>・名簿の新規追加、編集、削除<br>・ブロック・班や役職マスタの設定変更<br>・合言葉自体の変更、スプレッドシート連携URLの設定 |

### 🛡️ 役員交代時の引き継ぎ運用
年度が変わった時は、管理者がスプレッドシートの「システム設定」シート（またはWeb画面の設定タブ）で合言葉を新年度のものに変更するだけで、**旧役員の端末からのアクセスが一括遮断**されます。個人のGoogleアカウントを収集・管理する手間はありません。

---

## 📊 STEP 3: Googleスプレッドシートとの初回連携・データ同期（全員の手順）

自治会で管理しているGoogleスプレッドシートの名簿データを自分の端末（スマホ・PC）に読み込むため、初回に1回だけ連携URLの設定を行います。  
**一般役員の方も、管理者から案内されたURLをアプリに登録することで、最新の名簿データと同期できるようになります。**

### 📱【役員全員】アプリに連携URLを登録する手順（簡単3ステップ）
1. **Step 1:** 管理者からLINEやメール等で共有された**「Google Apps Script Web App URL」**（`https://script.google.com/macros/s/.../exec`）をコピーします。
2. **Step 2:** 名簿アプリの **「⚙️ 設定」** タブを開きます（PCは上部メニュー、スマホは下部ナビゲーション）。
3. **Step 3:** 「Google Apps Script Web App URL」の枠に貼り付け、**「保存して接続テスト」** ボタンをクリックします。

> ✅ 画面右上のインジケーターが「接続中（緑の点）」になれば完了です！自治会スプレッドシートの会員データが瞬時に同期されます。

### 🛠️【管理者向け】スプレッドシート側でのURL発行手順（初回のみ）
1. 自治会のGoogleスプレッドシートを開き、「拡張機能」>「Apps Script」を開きます。
2. 本システム同梱の `Code.js` を丸ごとコピー＆ペーストして保存します。
3. 右上の「デプロイ」>「新しいデプロイ」で「ウェブアプリ」を選択。
4. アクセスできるユーザーを **「全員」** に設定してデプロイします。
5. 発行された **Web App URL** をコピーし、役員グループ（LINE等）に案内してください。

---

## ❓ よくある質問（Q&A）

**Q. 役員の合言葉を忘れてしまいました。**  
A. 自治会長または名簿担当管理者にお問い合わせください。管理者はスプレッドシートの「システム設定」シート、または管理画面の設定タブから合言葉を確認できます。

**Q. スマホから名簿の文字を修正しようとするとボタンが押せません。**  
A. 現在「一般役員」としてログインしているため、安全のため閲覧専用モードになっています。名簿の修正が必要な場合は、右上の役員バッジをクリックして「管理者合言葉」で再ログインしてください。

**Q. 「接続エラー」と表示されて名簿が同期されません。**  
A. 「⚙️ 設定」タブに入力したURLの末尾が「/exec」になっているか確認してください。また、管理者がスプレッドシート側でデプロイした際のアクセス権限が「全員」になっているかご確認ください。

**Q. 屋外や集会所など電波が届きにくい場所でも使えますか？**  
A. はい、オフラインキャッシュ機能に対応しているため、通信が一時的に切れても手元の端末で名簿を確認できます。電波が回復した際に自動的に最新データと同期されます。
