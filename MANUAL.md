# 自治会員名簿管理システム 初回設定・利用マニュアル

本マニュアルは、自治会・町内会の役員向けに、会員名簿管理システムの初期設定から日常の操作方法までを解説したガイドブックです。

---

## 🌐 システム公開URL

- **WebアプリURL**:  
  [https://jack91709991-stack.github.io/community-roster/](https://jack91709991-stack.github.io/community-roster/)
- **配布・A4印刷用マニュアルページ**:  
  [https://jack91709991-stack.github.io/community-roster/manual.html](https://jack91709991-stack.github.io/community-roster/manual.html)

---

## 📲 STEP 1: スマホのホーム画面に追加（推奨）

本システムは、ブラウザだけで動く最新のWebアプリ（PWA）です。アプリストアからのインストール作業は不要です。

1. **iPhone（Safari）の場合**:  
   ブラウザ下部の「共有ボタン（四角から上矢印）」をタップ ➜ **「ホーム画面に追加」** を選択。
2. **Android（Chrome）の場合**:  
   右上のメニュー（縦の3点リーダー）をタップ ➜ **「ホーム画面に追加」** または **「アプリをインストール」** を選択。

> ホーム画面にアイコンが作成され、次回からアプリアイコンをタップするだけで全画面起動できます。

---

## 🔒 STEP 2: 初回起動時の「役員合言葉」の入力

個人情報保護のため、初回アクセス時や年度交代時には「役員認証」画面が表示されます。

| 権限ロール | 初期合言葉 | 主な権限・できること |
| :--- | :--- | :--- |
| **👑 管理者** | `admin2026` | **フル権限**<br>・名簿の新規追加、編集、削除<br>・ブロック・班や役職マスタの設定変更<br>・合言葉自体の変更、スプレッドシート連携URLの設定 |
| **👤 一般役員** | `roster2026` | **閲覧・集金・印刷専用**<br>・名簿の閲覧（電話・住所・家族構成等）<br>・「ブロック長・班長名簿」などのA4帳票印刷<br>・会費集金チェック（即時反映）<br>※名簿編集・削除や各種設定は誤操作防止のためロックされます |

### 🛡️ 役員交代時の引き継ぎ運用
年度が変わった時は、管理者がスプレッドシートの「システム設定」シート（またはWeb画面の設定タブ）で合言葉を新しい年度のもの（例: `roster2027`）に変更するだけで、**旧役員の端末からのアクセスが一括遮断**されます。個人のGoogleアカウントを収集・管理する手間はありません。

---

## 📊 STEP 3: Googleスプレッドシート連携（初回管理者のみ）

自治会のGoogleスプレッドシートとクラウド自動同期を行う手順です。

1. 自治会のGoogleスプレッドシートを開き、「拡張機能」>「Apps Script」を開きます。
2. 本システム同梱の `Code.js` を丸ごとコピー＆ペーストして保存します。
3. 右上の「デプロイ」>「新しいデプロイ」で「ウェブアプリ」を選択。
4. アクセスできるユーザーを **「全員」** に設定してデプロイします。
5. 発行された **Web App URL**（`https://script.google.com/macros/s/.../exec`）をコピーします。
6. 名簿アプリの「⚙️ 設定」タブを開き、URLを貼り付けて **「保存して接続テスト」** を押せば連携完了です！

---

## 👥 STEP 4: 日常の基本操作ガイド

### 1. 名簿一覧・検索
- **テーブル / カード切り替え**: 画面右上の「📑 テーブル」「🗂️ カード」ボタンで、用途に合わせて一覧表示を切り替えられます。
- **あいまい検索**: 検索バーに漢字・ひらがな・電話番号・住所の一部を入力するだけで瞬時に絞り込みができます。
- **ブロック・班・役員絞り込み**: ドロップダウンから目的のブロックや班だけを素早く抽出できます。

### 2. 会費集金チェック
1. 上部または下部の **「💴 会費集金」** タブを開きます。
2. 担当の班を選択します。
3. 会費を受け取った世帯の横にある **「未納」** ボタンをタップすると、1タップで **「納入済」**（緑色）に切り替わり、スプレッドシートにも即座に反映されます。
4. 集金進捗率（％）と集金金額がリアルタイムに自動集計されます。

### 3. 帳票印刷（ブロック長・班長名簿）
1. **「🖨️ 帳票印刷」** タブを開きます。
2. 「令和〇年度 ブロック長、班長名簿」がA4縦用紙に最適化されてプレビュー表示されます。
3. **「🖨️ この帳票を印刷する」** ボタンを押すと、A4用紙1ページに美しく収まる印刷ダイアログが開きます（PDF保存やコンビニ印刷も可能です）。

---

## ❓ よくある質問（Q&A）

**Q. 役員の合言葉を忘れてしまいました。**  
A. 自治会長または名簿担当管理者にお問い合わせください。管理者はスプレッドシートの「システム設定」シート、または管理画面の設定タブから合言葉を確認できます。

**Q. スマホから名簿の文字を修正しようとするとボタンが押せません。**  
A. 現在「一般役員」としてログインしているため、誤操作防止のため閲覧専用モードになっています。名簿の修正が必要な場合は、右上の役員バッジをクリックして「管理者合言葉」で再ログインしてください。

**Q. 屋外や集会所など電波が届きにくい場所でも使えますか？**  
A. はい、オフラインキャッシュ機能に対応しているため、通信が一時的に切れても手元の端末で名簿を確認できます。電波が回復した際に自動的に最新データと同期されます。
